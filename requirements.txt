pandas
numpy
scikit-learn
xgboost
flask
joblib
gunicorn
requests
# mlflow is pre-installed in Cloudera AI via mlflow-cml-plugin.
# Do NOT add mlflow here — installing a newer version breaks the CML plugin.
